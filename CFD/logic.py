
""" 
        score = customer_cfd(user_query, key_words)

        if score >= 1:
            
            f.write("\n" + cust + ": " + user_query + " : " + customer_mapping[cust])

        display_cfd(user_query,score)

        f.close()
"""

